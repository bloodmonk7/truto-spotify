# Build Setup

```bash
# Clone this repository
$ git clone https://github.com/bloodmonk7/truto-spotify.git
# Go into the repository
$ cd truto-spotify
# install dependencies
$ npm install

# serve with hot reload at http://localhost:5173
$ npm run dev


```
